;; std/llang/ast -- the l-lang AST SCHEMA, in l-lang.
;;
;; GENERATED FROM `src/compiler/frontend/ast.ts` BY `npm run ast:stdlib`. Do not edit by hand;
;; `npm run test:docs` regenerates it and fails if the committed copy has drifted.
;;
;; A quoted form is a MAP (D3d) whose `_type` names its kind and whose other keys are that
;; kind's declared fields. Reading a field is ordinary member access and is already TOTAL --
;; `n.condition` on a `list` answers nil rather than raising -- so this module deliberately
;; declares NO field accessors. What it declares is what member access cannot tell you:
;;
;;   * which kinds exist at all             -- `kinds`, and one predicate per kind
;;   * what fields a kind declares          -- `fields-of`
;;   * which of those hold CHILD NODES      -- `child-fields-of`, what a walker follows
;;
;; Every accessor is TOTAL: an unknown kind answers an empty vector, never an error.
;;
;; 88 node kinds.
(
    ;; The kind of a datum, or nil if it is not a node.
    (fn node-type [n <- Any] -> String? (return n._type))

    ;; Every node kind the parser can build, sorted.
    (fn kinds [] -> String[] (return ["any-pattern" "attribute-def" "await" "binary-number" "boolean" "call" "cast" "class" "comment" "complex-number" "composite-identifier" "compound-assignment" "cond" "cond-case" "constant-pattern" "dimension-refinement" "enum" "enum-key" "export" "extends" "float-number" "for" "for-each" "format-expression" "formatted-string" "fraction-number" "function" "function-type" "functional-pattern" "generic-type" "handle" "hex-number" "identifier-pattern" "if" "implements" "import" "indexer" "integer-number" "interface" "intersection-type" "invoke-restart" "key-value" "list" "list-pattern" "macro-def" "map" "map-key-type" "map-pattern" "map-pattern-pair" "map-type" "mapped-type" "match" "match-case" "matrix" "member" "modifier" "modifier-def" "null" "octal-number" "parameter" "program" "quasiquote" "quote" "range-refinement" "rest-pattern" "restart-case" "signal" "simple-assignment" "simple-identifier" "simple-type" "spread" "string" "struct" "try-catch" "tuple-type" "type" "type-constraint" "type-def" "type-guard" "type-name" "type-pattern" "union-type" "unquote" "variable" "vector" "vector-pattern" "when" "while"]))

    ;; Is this the name of a kind the parser can build?
    (fn kind-exists [k <- String] -> Boolean
        (match k {
            "any-pattern" => (return true)
            "attribute-def" => (return true)
            "await" => (return true)
            "binary-number" => (return true)
            "boolean" => (return true)
            "call" => (return true)
            "cast" => (return true)
            "class" => (return true)
            "comment" => (return true)
            "complex-number" => (return true)
            "composite-identifier" => (return true)
            "compound-assignment" => (return true)
            "cond" => (return true)
            "cond-case" => (return true)
            "constant-pattern" => (return true)
            "dimension-refinement" => (return true)
            "enum" => (return true)
            "enum-key" => (return true)
            "export" => (return true)
            "extends" => (return true)
            "float-number" => (return true)
            "for" => (return true)
            "for-each" => (return true)
            "format-expression" => (return true)
            "formatted-string" => (return true)
            "fraction-number" => (return true)
            "function" => (return true)
            "function-type" => (return true)
            "functional-pattern" => (return true)
            "generic-type" => (return true)
            "handle" => (return true)
            "hex-number" => (return true)
            "identifier-pattern" => (return true)
            "if" => (return true)
            "implements" => (return true)
            "import" => (return true)
            "indexer" => (return true)
            "integer-number" => (return true)
            "interface" => (return true)
            "intersection-type" => (return true)
            "invoke-restart" => (return true)
            "key-value" => (return true)
            "list" => (return true)
            "list-pattern" => (return true)
            "macro-def" => (return true)
            "map" => (return true)
            "map-key-type" => (return true)
            "map-pattern" => (return true)
            "map-pattern-pair" => (return true)
            "map-type" => (return true)
            "mapped-type" => (return true)
            "match" => (return true)
            "match-case" => (return true)
            "matrix" => (return true)
            "member" => (return true)
            "modifier" => (return true)
            "modifier-def" => (return true)
            "null" => (return true)
            "octal-number" => (return true)
            "parameter" => (return true)
            "program" => (return true)
            "quasiquote" => (return true)
            "quote" => (return true)
            "range-refinement" => (return true)
            "rest-pattern" => (return true)
            "restart-case" => (return true)
            "signal" => (return true)
            "simple-assignment" => (return true)
            "simple-identifier" => (return true)
            "simple-type" => (return true)
            "spread" => (return true)
            "string" => (return true)
            "struct" => (return true)
            "try-catch" => (return true)
            "tuple-type" => (return true)
            "type" => (return true)
            "type-constraint" => (return true)
            "type-def" => (return true)
            "type-guard" => (return true)
            "type-name" => (return true)
            "type-pattern" => (return true)
            "union-type" => (return true)
            "unquote" => (return true)
            "variable" => (return true)
            "vector" => (return true)
            "vector-pattern" => (return true)
            "when" => (return true)
            "while" => (return true)
            _ => (return false)
        }))

    ;; The declared field names of a kind, in declaration order -- [] for an unknown kind,
    ;; never an error.
    (fn fields-of [k <- String] -> String[]
        (match k {
            "any-pattern" => (return [])
            "attribute-def" => (return ["name" "params"])
            "await" => (return ["expression"])
            "binary-number" => (return ["match" "value"])
            "boolean" => (return ["value"])
            "call" => (return ["callee" "arguments"])
            "cast" => (return ["target" "value"])
            "class" => (return ["name" "modifiers" "implements" "extends" "generics" "body"])
            "comment" => (return ["comment"])
            "complex-number" => (return ["match" "real" "imaginary"])
            "composite-identifier" => (return ["id" "headless" "parts"])
            "compound-assignment" => (return ["assignable" "value" "operator"])
            "cond" => (return ["cases"])
            "cond-case" => (return ["condition" "body"])
            "constant-pattern" => (return ["constant" "regexSugar"])
            "dimension-refinement" => (return ["op" "operands"])
            "enum" => (return ["name" "modifiers" "body"])
            "enum-key" => (return ["key" "value"])
            "export" => (return ["exports"])
            "extends" => (return ["type" "generics"])
            "float-number" => (return ["match" "value"])
            "for" => (return ["initial" "condition" "step" "then" "else" "duplicateClauses"])
            "for-each" => (return ["variable" "collection" "then" "else" "duplicateClauses"])
            "format-expression" => (return ["expression"])
            "formatted-string" => (return ["value"])
            "fraction-number" => (return ["match" "numerator" "denominator"])
            "function" => (return ["name" "async" "generator" "extern" "modifiers" "params" "returns" "body" "castOf" "generics"])
            "function-type" => (return ["params" "ret"])
            "functional-pattern" => (return ["params" "ret"])
            "generic-type" => (return ["name" "generics"])
            "handle" => (return ["body" "clauses"])
            "hex-number" => (return ["match" "value"])
            "identifier-pattern" => (return ["id"])
            "if" => (return ["condition" "then" "else"])
            "implements" => (return ["type" "generics"])
            "import" => (return ["imports"])
            "indexer" => (return ["id" "indices" "members"])
            "integer-number" => (return ["match" "value"])
            "interface" => (return ["name" "generics" "modifiers" "implements" "body"])
            "intersection-type" => (return ["types"])
            "invoke-restart" => (return ["name" "args"])
            "key-value" => (return ["key" "value"])
            "list" => (return ["nodes"])
            "list-pattern" => (return ["elements"])
            "macro-def" => (return ["keyword" "name" "body"])
            "map" => (return ["values"])
            "map-key-type" => (return ["key" "type"])
            "map-pattern" => (return ["pairs"])
            "map-pattern-pair" => (return ["key" "pattern"])
            "map-type" => (return ["keys"])
            "mapped-type" => (return ["mapping"])
            "match" => (return ["expression" "cases"])
            "match-case" => (return ["pattern" "guard" "body"])
            "matrix" => (return ["rows"])
            "member" => (return ["object" "property" "computed"])
            "modifier" => (return ["modifier" "args"])
            "modifier-def" => (return ["name" "params" "body"])
            "null" => (return ["keyword"])
            "octal-number" => (return ["match" "value"])
            "parameter" => (return ["name" "modifiers" "type" "spread"])
            "program" => (return ["program"])
            "quasiquote" => (return ["nodes"])
            "quote" => (return ["nodes"])
            "range-refinement" => (return ["lo" "hi"])
            "rest-pattern" => (return ["id"])
            "restart-case" => (return ["body" "arms"])
            "signal" => (return ["condition"])
            "simple-assignment" => (return ["assignable" "value"])
            "simple-identifier" => (return ["id"])
            "simple-type" => (return ["name"])
            "spread" => (return ["expression"])
            "string" => (return ["value"])
            "struct" => (return ["name" "modifiers" "implements" "extends" "body"])
            "try-catch" => (return ["try" "catch" "finally"])
            "tuple-type" => (return ["elements"])
            "type" => (return ["type" "array" "optional"])
            "type-constraint" => (return ["where" "constraints"])
            "type-def" => (return ["name" "type" "modifiers" "refinement"])
            "type-guard" => (return ["value" "type"])
            "type-name" => (return ["name" "variance"])
            "type-pattern" => (return ["id" "type"])
            "union-type" => (return ["types"])
            "unquote" => (return ["expression"])
            "variable" => (return ["name" "mutable" "extern" "modifiers" "type" "value"])
            "vector" => (return ["values"])
            "vector-pattern" => (return ["elements"])
            "when" => (return ["condition" "then"])
            "while" => (return ["condition" "then"])
            _ => (return [])
        }))

    ;; The fields of a kind that hold CHILD NODES -- what a walker follows. [] for unknown.
    (fn child-fields-of [k <- String] -> String[]
        (match k {
            "any-pattern" => (return [])
            "attribute-def" => (return ["params"])
            "await" => (return ["expression"])
            "binary-number" => (return [])
            "boolean" => (return [])
            "call" => (return ["callee" "arguments"])
            "cast" => (return ["target" "value"])
            "class" => (return ["name" "modifiers" "implements" "extends" "generics" "body"])
            "comment" => (return [])
            "complex-number" => (return [])
            "composite-identifier" => (return [])
            "compound-assignment" => (return ["assignable" "value"])
            "cond" => (return ["cases"])
            "cond-case" => (return ["condition" "body"])
            "constant-pattern" => (return ["constant"])
            "dimension-refinement" => (return ["operands"])
            "enum" => (return ["name" "modifiers" "body"])
            "enum-key" => (return ["key" "value"])
            "export" => (return [])
            "extends" => (return ["type" "generics"])
            "float-number" => (return [])
            "for" => (return ["initial" "condition" "step" "then" "else"])
            "for-each" => (return ["variable" "collection" "then" "else"])
            "format-expression" => (return ["expression"])
            "formatted-string" => (return ["value"])
            "fraction-number" => (return [])
            "function" => (return ["name" "modifiers" "params" "returns" "body" "generics"])
            "function-type" => (return ["params" "ret"])
            "functional-pattern" => (return ["params" "ret"])
            "generic-type" => (return ["name" "generics"])
            "handle" => (return ["body"])
            "hex-number" => (return [])
            "identifier-pattern" => (return ["id"])
            "if" => (return ["condition" "then" "else"])
            "implements" => (return ["type" "generics"])
            "import" => (return [])
            "indexer" => (return ["id" "indices"])
            "integer-number" => (return [])
            "interface" => (return ["name" "generics" "modifiers" "implements" "body"])
            "intersection-type" => (return ["types"])
            "invoke-restart" => (return ["args"])
            "key-value" => (return ["key" "value"])
            "list" => (return ["nodes"])
            "list-pattern" => (return ["elements"])
            "macro-def" => (return ["name" "body"])
            "map" => (return ["values"])
            "map-key-type" => (return ["key" "type"])
            "map-pattern" => (return ["pairs"])
            "map-pattern-pair" => (return ["key" "pattern"])
            "map-type" => (return ["keys"])
            "mapped-type" => (return ["mapping"])
            "match" => (return ["expression" "cases"])
            "match-case" => (return ["pattern" "guard" "body"])
            "matrix" => (return ["rows"])
            "member" => (return ["object" "property"])
            "modifier" => (return ["args"])
            "modifier-def" => (return ["params" "body"])
            "null" => (return [])
            "octal-number" => (return [])
            "parameter" => (return ["name" "modifiers" "type"])
            "program" => (return ["program"])
            "quasiquote" => (return ["nodes"])
            "quote" => (return ["nodes"])
            "range-refinement" => (return ["lo" "hi"])
            "rest-pattern" => (return ["id"])
            "restart-case" => (return ["body"])
            "signal" => (return ["condition"])
            "simple-assignment" => (return ["assignable" "value"])
            "simple-identifier" => (return [])
            "simple-type" => (return ["name"])
            "spread" => (return ["expression"])
            "string" => (return [])
            "struct" => (return ["name" "modifiers" "implements" "extends" "body"])
            "try-catch" => (return ["try" "finally"])
            "tuple-type" => (return ["elements"])
            "type" => (return ["type"])
            "type-constraint" => (return ["where" "constraints"])
            "type-def" => (return ["name" "type" "modifiers" "refinement"])
            "type-guard" => (return ["value" "type"])
            "type-name" => (return [])
            "type-pattern" => (return ["id" "type"])
            "union-type" => (return ["types"])
            "unquote" => (return ["expression"])
            "variable" => (return ["name" "modifiers" "type" "value"])
            "vector" => (return ["values"])
            "vector-pattern" => (return ["elements"])
            "when" => (return ["condition" "then"])
            "while" => (return ["condition" "then"])
            _ => (return [])
        }))

    ;; -- one predicate per kind ----------------------------------------------------------

    (fn is-any-pattern [n <- Any] -> Boolean (return (== n._type "any-pattern")))
    (fn is-attribute-def [n <- Any] -> Boolean (return (== n._type "attribute-def")))
    (fn is-await [n <- Any] -> Boolean (return (== n._type "await")))
    (fn is-binary-number [n <- Any] -> Boolean (return (== n._type "binary-number")))
    (fn is-boolean [n <- Any] -> Boolean (return (== n._type "boolean")))
    (fn is-call [n <- Any] -> Boolean (return (== n._type "call")))
    (fn is-cast [n <- Any] -> Boolean (return (== n._type "cast")))
    (fn is-class [n <- Any] -> Boolean (return (== n._type "class")))
    (fn is-comment [n <- Any] -> Boolean (return (== n._type "comment")))
    (fn is-complex-number [n <- Any] -> Boolean (return (== n._type "complex-number")))
    (fn is-composite-identifier [n <- Any] -> Boolean (return (== n._type "composite-identifier")))
    (fn is-compound-assignment [n <- Any] -> Boolean (return (== n._type "compound-assignment")))
    (fn is-cond [n <- Any] -> Boolean (return (== n._type "cond")))
    (fn is-cond-case [n <- Any] -> Boolean (return (== n._type "cond-case")))
    (fn is-constant-pattern [n <- Any] -> Boolean (return (== n._type "constant-pattern")))
    (fn is-dimension-refinement [n <- Any] -> Boolean (return (== n._type "dimension-refinement")))
    (fn is-enum [n <- Any] -> Boolean (return (== n._type "enum")))
    (fn is-enum-key [n <- Any] -> Boolean (return (== n._type "enum-key")))
    (fn is-export [n <- Any] -> Boolean (return (== n._type "export")))
    (fn is-extends [n <- Any] -> Boolean (return (== n._type "extends")))
    (fn is-float-number [n <- Any] -> Boolean (return (== n._type "float-number")))
    (fn is-for [n <- Any] -> Boolean (return (== n._type "for")))
    (fn is-for-each [n <- Any] -> Boolean (return (== n._type "for-each")))
    (fn is-format-expression [n <- Any] -> Boolean (return (== n._type "format-expression")))
    (fn is-formatted-string [n <- Any] -> Boolean (return (== n._type "formatted-string")))
    (fn is-fraction-number [n <- Any] -> Boolean (return (== n._type "fraction-number")))
    (fn is-function [n <- Any] -> Boolean (return (== n._type "function")))
    (fn is-function-type [n <- Any] -> Boolean (return (== n._type "function-type")))
    (fn is-functional-pattern [n <- Any] -> Boolean (return (== n._type "functional-pattern")))
    (fn is-generic-type [n <- Any] -> Boolean (return (== n._type "generic-type")))
    (fn is-handle [n <- Any] -> Boolean (return (== n._type "handle")))
    (fn is-hex-number [n <- Any] -> Boolean (return (== n._type "hex-number")))
    (fn is-identifier-pattern [n <- Any] -> Boolean (return (== n._type "identifier-pattern")))
    (fn is-if [n <- Any] -> Boolean (return (== n._type "if")))
    (fn is-implements [n <- Any] -> Boolean (return (== n._type "implements")))
    (fn is-import [n <- Any] -> Boolean (return (== n._type "import")))
    (fn is-indexer [n <- Any] -> Boolean (return (== n._type "indexer")))
    (fn is-integer-number [n <- Any] -> Boolean (return (== n._type "integer-number")))
    (fn is-interface [n <- Any] -> Boolean (return (== n._type "interface")))
    (fn is-intersection-type [n <- Any] -> Boolean (return (== n._type "intersection-type")))
    (fn is-invoke-restart [n <- Any] -> Boolean (return (== n._type "invoke-restart")))
    (fn is-key-value [n <- Any] -> Boolean (return (== n._type "key-value")))
    (fn is-list [n <- Any] -> Boolean (return (== n._type "list")))
    (fn is-list-pattern [n <- Any] -> Boolean (return (== n._type "list-pattern")))
    (fn is-macro-def [n <- Any] -> Boolean (return (== n._type "macro-def")))
    (fn is-map [n <- Any] -> Boolean (return (== n._type "map")))
    (fn is-map-key-type [n <- Any] -> Boolean (return (== n._type "map-key-type")))
    (fn is-map-pattern [n <- Any] -> Boolean (return (== n._type "map-pattern")))
    (fn is-map-pattern-pair [n <- Any] -> Boolean (return (== n._type "map-pattern-pair")))
    (fn is-map-type [n <- Any] -> Boolean (return (== n._type "map-type")))
    (fn is-mapped-type [n <- Any] -> Boolean (return (== n._type "mapped-type")))
    (fn is-match [n <- Any] -> Boolean (return (== n._type "match")))
    (fn is-match-case [n <- Any] -> Boolean (return (== n._type "match-case")))
    (fn is-matrix [n <- Any] -> Boolean (return (== n._type "matrix")))
    (fn is-member [n <- Any] -> Boolean (return (== n._type "member")))
    (fn is-modifier [n <- Any] -> Boolean (return (== n._type "modifier")))
    (fn is-modifier-def [n <- Any] -> Boolean (return (== n._type "modifier-def")))
    (fn is-null [n <- Any] -> Boolean (return (== n._type "null")))
    (fn is-octal-number [n <- Any] -> Boolean (return (== n._type "octal-number")))
    (fn is-parameter [n <- Any] -> Boolean (return (== n._type "parameter")))
    (fn is-program [n <- Any] -> Boolean (return (== n._type "program")))
    (fn is-quasiquote [n <- Any] -> Boolean (return (== n._type "quasiquote")))
    (fn is-quote [n <- Any] -> Boolean (return (== n._type "quote")))
    (fn is-range-refinement [n <- Any] -> Boolean (return (== n._type "range-refinement")))
    (fn is-rest-pattern [n <- Any] -> Boolean (return (== n._type "rest-pattern")))
    (fn is-restart-case [n <- Any] -> Boolean (return (== n._type "restart-case")))
    (fn is-signal [n <- Any] -> Boolean (return (== n._type "signal")))
    (fn is-simple-assignment [n <- Any] -> Boolean (return (== n._type "simple-assignment")))
    (fn is-simple-identifier [n <- Any] -> Boolean (return (== n._type "simple-identifier")))
    (fn is-simple-type [n <- Any] -> Boolean (return (== n._type "simple-type")))
    (fn is-spread [n <- Any] -> Boolean (return (== n._type "spread")))
    (fn is-string [n <- Any] -> Boolean (return (== n._type "string")))
    (fn is-struct [n <- Any] -> Boolean (return (== n._type "struct")))
    (fn is-try-catch [n <- Any] -> Boolean (return (== n._type "try-catch")))
    (fn is-tuple-type [n <- Any] -> Boolean (return (== n._type "tuple-type")))
    (fn is-type [n <- Any] -> Boolean (return (== n._type "type")))
    (fn is-type-constraint [n <- Any] -> Boolean (return (== n._type "type-constraint")))
    (fn is-type-def [n <- Any] -> Boolean (return (== n._type "type-def")))
    (fn is-type-guard [n <- Any] -> Boolean (return (== n._type "type-guard")))
    (fn is-type-name [n <- Any] -> Boolean (return (== n._type "type-name")))
    (fn is-type-pattern [n <- Any] -> Boolean (return (== n._type "type-pattern")))
    (fn is-union-type [n <- Any] -> Boolean (return (== n._type "union-type")))
    (fn is-unquote [n <- Any] -> Boolean (return (== n._type "unquote")))
    (fn is-variable [n <- Any] -> Boolean (return (== n._type "variable")))
    (fn is-vector [n <- Any] -> Boolean (return (== n._type "vector")))
    (fn is-vector-pattern [n <- Any] -> Boolean (return (== n._type "vector-pattern")))
    (fn is-when [n <- Any] -> Boolean (return (== n._type "when")))
    (fn is-while [n <- Any] -> Boolean (return (== n._type "while")))

    (export
        node-type kinds kind-exists fields-of child-fields-of
        is-any-pattern is-attribute-def is-await is-binary-number is-boolean is-call is-cast
        is-class is-comment is-complex-number is-composite-identifier is-compound-assignment
        is-cond is-cond-case is-constant-pattern is-dimension-refinement is-enum is-enum-key
        is-export is-extends is-float-number is-for is-for-each is-format-expression
        is-formatted-string is-fraction-number is-function is-function-type is-functional-pattern
        is-generic-type is-handle is-hex-number is-identifier-pattern is-if is-implements
        is-import is-indexer is-integer-number is-interface is-intersection-type is-invoke-restart
        is-key-value is-list is-list-pattern is-macro-def is-map is-map-key-type is-map-pattern
        is-map-pattern-pair is-map-type is-mapped-type is-match is-match-case is-matrix is-member
        is-modifier is-modifier-def is-null is-octal-number is-parameter is-program is-quasiquote
        is-quote is-range-refinement is-rest-pattern is-restart-case is-signal
        is-simple-assignment is-simple-identifier is-simple-type is-spread is-string is-struct
        is-try-catch is-tuple-type is-type is-type-constraint is-type-def is-type-guard
        is-type-name is-type-pattern is-union-type is-unquote is-variable is-vector
        is-vector-pattern is-when is-while)
)
